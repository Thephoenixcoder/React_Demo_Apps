import React from 'react'

export default function Header() {
    return (
        <header style={{ padding: '20px', backgroundColor: "#000", color: '#fff', marginBottom: '2rem' }}>
            <h1>To do list</h1>
        </header>
    )
}
